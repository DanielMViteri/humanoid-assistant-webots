[project]
name = "swarmsense"
version = "0.1.0"
description = "Simulated elderly-care humanoid assistant (voice, perception, Webots, MongoDB)."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
